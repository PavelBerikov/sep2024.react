import {FC} from 'react';
import LoginForm from '../components/forms/LoginForm';

const LoginPage:FC = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;