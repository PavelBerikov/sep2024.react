import { FC } from "react";
import Posts from "../components/posts/Posts";

const PostsPage: FC = () => {
    return (
        <div>
            <Posts/>
        </div>
    );
};

export default PostsPage;